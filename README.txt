1. Create a .txt file and write something inside
2. Run the Server
3. Run the Client
4. Send the created .txt file (You should always create a file before running the client)
5. Run the client again and receive the sent .txt file
6. Check to see if the file is correctly transferred.


*If you don't see anything written on the sent one, you should rerun the client and send again.